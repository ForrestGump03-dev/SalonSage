# Database Configuration
# Per il test, usiamo un database PostgreSQL pubblico di test
# In produzione, sostituisci con il tuo DATABASE_URL reale di Neon o PostgreSQL locale
DATABASE_URL="postgresql://test:test@localhost:5432/test"

# Server Configuration
PORT=3000

# Environment
NODE_ENV=development
